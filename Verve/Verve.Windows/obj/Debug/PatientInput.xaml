<Page
    x:Name="pageRoot"
    x:Class="Verve.PatientInput"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Verve"
    xmlns:common="using:Verve.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">Patient's History</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid Background="#FF0D3216">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40" FontFamily="Georgia"/>
        </Grid>
        <TextBlock HorizontalAlignment="Left" Margin="77,69,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Please enter your unique ID." VerticalAlignment="Top" Height="62" Width="367" FontFamily="Georgia" FontSize="24"/>
        <TextBlock HorizontalAlignment="Left" Margin="77,165,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Patient's ID:-" VerticalAlignment="Top" Width="145" Height="57" FontSize="24" FontFamily="Georgia"/>
        <TextBox x:ConnectionId='1' x:Name="sup" HorizontalAlignment="Left" Margin="245,165,0,0" Grid.Row="1" TextWrapping="Wrap" Text="" MaxLength="10" VerticalAlignment="Top" Width="213"                                  />
        <Button x:ConnectionId='2' Content="Submit" HorizontalAlignment="Left" Margin="349,217,0,0" Grid.Row="1" VerticalAlignment="Top"              FontSize="24" FontFamily="Georgia" FontWeight="Normal"/>
    </Grid>
</Page>

